import React from "react";

const MultipleMediaInput = ({
  files,
  setFiles,
}: {
  files: File[];
  setFiles: React.Dispatch<React.SetStateAction<File[]>>;
}) => {
  return <div>MultipleMediaInput</div>;
};

export default MultipleMediaInput;
